[package]
name = "workshop-status"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true

description = "Workshop status subsystem: the status-bar broadcast bus and the progress renderer that drives the bar's indicator from the process progress hub"

[dependencies]
shared-progress.workspace = true
tokio.workspace = true
workshop-protocol.workspace = true
workshop-registry.workspace = true
workshop-support.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "test-util"] }

[lints]
workspace = true
