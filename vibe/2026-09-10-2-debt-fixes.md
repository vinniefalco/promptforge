---
name: Debt Fixes - Input Tool and Version Gate
overview: "Remove two accepted debts from the unified prompt model run: narrow the model-visible input tool's advertised contract to its real behavior (DEBT-UPM-02), and flip the version gate to `promptforge: 0` only with migration of every existing prompt (DEBT-UPM-04). DEBT-UPM-01 is operator-accepted without a fix; DEBT-UPM-03 gets no interim fix because the planned database-driven system will own agent memory."
todos:
  - id: fix-input-tool
    content: Narrow the input tool description and schema in input.rs to its real behavior; update affected tests.
    status: pending
  - id: flip-gate
    content: "Flip the version gate to `promptforge: 0` only and clean up `SUPPORTED_MAJOR`."
    status: pending
  - id: migrate-prompts
    content: "Migrate every `promptforge: 1` string to `promptforge: 0` repo-wide, inline test strings included; historical `vibe/` records excluded."
    status: pending
  - id: gate-tests
    content: Invert version-acceptance tests into rejection tests and add the gate behavior test.
    status: pending
  - id: verify
    content: Run focused tests, then the full gate (build, fmt, clippy, workspace suite).
    status: pending
  - id: close-out
    content: Commit one commit per debt and annotate the debt removal plan's execution items.
    status: pending
isProject: false
---

# Debt Fixes - Input Tool and Version Gate

<product-contract>

## Product Requirements

- Problem and users:
  - The model-visible `user_input` tool advertises a `question` parameter that the implementation silently discards, so a model can compose a question the operator never sees. Prompt authors and their models need the advertised tool contract to match behavior.
  - The executor's version gate admits `promptforge: 1` documents under a temporary dual-read transition, but only version-0 semantics exist; those documents fail mid-run with confusing errors instead of a clean rejection. Authors need incompatible documents rejected at the gate.
- Goals:
  - The input tool's advertised schema and description state only what the tool does.
  - The version gate accepts `promptforge: 0` only.
  - Every existing prompt document and inline test string declares `promptforge: 0`.
- Non-goals:
  - Do not extend `InputBroker` with a question channel; that is a real feature for a later host-interaction pass.
  - Do not fix DEBT-UPM-01 (raw-id fallback context window); the operator accepts it as pre-product debt.
  - Do not build an interim history restore for DEBT-UPM-03; the planned database-driven system will own agent memory.
  - Do not edit historical records under `vibe/`; dated design documents and the ledger keep their historical `promptforge: 1` mentions.
- Success criteria:
  - A `promptforge: 1` document is rejected at run start with the typed unsupported-version error.
  - The input tool's advertised schema carries no `question` property and its description makes no ask-a-question promise.
  - No `promptforge: 1` string remains outside `vibe/`.
  - All affected crate suites, clippy with `-D warnings`, and `cargo fmt --all --check` pass.
- Constraints:
  - `promptforge: 0` is the target; never propose incrementing it.
  - Land as two commits, one per debt.
- Open questions:
  - None.

## Functional Specification

- Actors and workflows:
  - The input broker backs the script-side `user_input()` function only; no `user_input` tool is advertised to the model in a brokered `models.loop` scope. (Corrected 2026-09-12: the original statement described an unintended synthetic tool injection, since removed.)
  - An author or test runs a prompt document; the executor checks the frontmatter version before any section runs.
- Inputs and outputs:
  - The input tool advertises an empty properties object and the description "Give the user the opportunity to add a prompt and wait for their typed input."
  - The version gate accepts exactly `promptforge: 0`.
- States and validation:
  - Version validation happens once at run start, before section execution.
- Errors and recovery:
  - A `promptforge: 1` or other non-zero document fails with the existing typed `UnsupportedVersion` error naming the declared version.
  - A document with no version fails with the existing structure parse error.
- Security and privacy behavior:
  - No change.
- Acceptance criteria:
  - A behavior test rejects a `promptforge: 1` document at the gate.
  - A schema test asserts the input tool advertises no `question` property.
  - Migrated inline test suites pass unchanged in every other respect.

</product-contract>

<implementation-contract>

## Technical Design

- Architecture:
  - No architectural change. The input tool's wire contract narrows to its existing behavior; the version gate narrows to the only implemented semantics.
- Modules and interfaces:
  - `crates/promptforge-core/src/input.rs`: `InputTool::description` becomes "Give the user the opportunity to add a prompt and wait for their typed input."; `parameters_schema` drops the `question` property, leaving an empty properties object; the `call` doc comment no longer mentions a question channel.
  - `crates/promptforge-core/src/execute.rs`: the gate arm `Some(SUPPORTED_MAJOR | 0) => {}` becomes `Some(0) => {}` and the dual-read comment is deleted.
  - `crates/promptforge-core/src/execute/support.rs`: `SUPPORTED_MAJOR` is removed when no use remains, or repurposed to name the zero gate.
  - `crates/promptforge-parser`: confirm the parser's own version handling agrees with the 0-only gate.
- File and public API changes:
  - Migrate every `promptforge: 1` occurrence to `promptforge: 0`: `crates/promptforge-core/src/execute/tests/` (`scheduler.rs` 91, `model_and_reply.rs` 24, `exit_rules.rs` 9, `debug_and_counts.rs` 8, `live_infer.rs` 14, `observations.rs` 4, `mod.rs` 4, `input.rs` 1, `models_loop.rs` 1, `local_tools.rs` 2, `lazy_prose.rs` 1, `exec_flow.rs` 1), `crates/promptforge-core/src/execute/context.rs` (1), `crates/promptforge-core/src/observe.rs` (1), `crates/promptforge-parser/src/tests.rs` (7).
  - Tests that assert a `promptforge: 1` document is accepted are inverted to assert typed rejection.
- Data, persistence, failure, security, and privacy constraints:
  - The gate flip changes which persisted prompt documents are accepted; that is the point of the fix and is operator-resolved.

</implementation-contract>

<verification-contract>

## Testing Plan

- Unit:
  - Gate test: a `promptforge: 1` document fails with `UnsupportedVersion`; a `promptforge: 0` document runs; a versionless document fails with the structure error.
  - Schema test: the input tool's advertised properties carry no `question` and the description makes no ask-a-question promise.
  - Existing version-rejection tests (`promptforge: 2` and unknown versions) keep passing.
- Integration and end-to-end:
  - The migrated inline suites in `promptforge-core` and `promptforge-parser` pass under the 0-only gate.
  - `cargo test -p promptforge-lua` and the workshop-server suite confirm no consumer depended on the old schema or gate.
- Regression, security, and performance:
  - No performance-sensitive paths touched.
- Exit criteria:
  - `cargo build`, `cargo fmt --all --check`, clippy with `-D warnings` (workspace and workshop variants), and the full workspace suite pass per the project survey's documented commands and host fallback.

</verification-contract>

<decision-record>

## Decision Record

- Decisions:
  - Narrow the input tool's advertised contract to its behavior rather than plumbing the question through. The operator decided: "The model never calls that, only the Lua. The tool should be described as 'Give the user the opportunity to add a prompt' or something (figure it out)."
  - Flip the version gate to `promptforge: 0` only and migrate every prompt, including inline test scaffolding. The operator decided: "yes flip the fate to 0, fix every existing prompt that you can find" (read "fate" as "gate").
  - Accept DEBT-UPM-01 (raw-id fallback context window) without a fix. The operator decided: "its not a big deal. no one is really using this yet, we are still building it."
  - Give DEBT-UPM-03 (relaunch amnesia) no interim fix. The operator decided: "we are going to build a very robust database-driven system and when we do that, then we will save the agent memory."
  - Land the two fixes as two commits, one per debt.
- Adjudication outcome:
  - DEBT-UPM-01 (raw-id fallback context window): accepted, not fixed - pre-product, low priority.
  - DEBT-UPM-02 (false `question` contract): fixed, landed as `df7a7615` "Narrow the input tool contract to its real behavior".
  - DEBT-UPM-03 (relaunch amnesia): interim fix declined; the future database-driven persistence system owns agent memory.
  - DEBT-UPM-04 (dual-read version gate): fixed, landed as `2178ce16` "Gate prompt execution to promptforge major 0 only".
- Rejected alternatives:
  - Reject extending `InputBroker::user_input` with a question parameter because it is a public trait change for a feature no current consumer uses; revisit when a host-interaction pass designs the question channel.
  - Reject the interim host-seeded history rebuild for DEBT-UPM-03 because the database-driven persistence system will own agent memory; revisit only if that system is cancelled.
  - Reject migrating historical `vibe/` documents because dated design records must keep their historical contents; revisit never.
- Assumptions, risks, and notes:
  - Any un-migrated `promptforge: 1` document in the wild now fails at the gate instead of mid-run; that is the intended behavior change.
  - The parser crate has its own version handling; the migration keeps it consistent with the 0-only gate.

</decision-record>

<project-survey>

## Project Survey

- Status: complete
- Build command: `cargo build` (default member is the gateway; it compiles on a fresh clone with no CUDA toolkit or Tauri system packages). The desktop app is an explicit choice: `cargo build -p workshop`. UI bundles build through crate build scripts and require `npm ci --prefix crates/workshop-server/ui` and `npm ci --prefix crates/gateway-config-ui/ui` first.
- Focused test command pattern: `cargo nextest run -p <crate> <filter>` for a single test or filter within one crate; `cargo test -p <crate> --test it <filter>` for a single integration test inside a crate's `it` target (CI uses this form, e.g. `cargo test -p gateway-stt --test it architecture`).
- Component test command pattern: `cargo nextest run -p <crate>` for one crate's full suite; some gateway process-ownership tests require `cargo test --locked -p gateway --no-default-features --features test-fixtures --test it <name>`.
- Full-suite test command: `cargo nextest run --locked --workspace --exclude workshop --exclude workshop-server --all-features`, then doctests via `cargo test --workspace --exclude workshop --exclude workshop-server --all-features --doc`. Workshop crates test separately on Windows: `cargo nextest run --locked -p workshop -p workshop-server`. On hosts without cargo-nextest, the fallback is `cargo test --locked --workspace --exclude workshop --exclude workshop-server --all-features` and `cargo test --locked -p workshop -p workshop-server`.
- Linter command: `cargo clippy --workspace --exclude workshop --exclude workshop-server --all-targets --all-features -- -D warnings` (workshop crates: `cargo clippy -p workshop -p workshop-server --all-targets -- -D warnings`).
- Formatter check command: `cargo fmt --all --check`.
- Test placement and naming conventions: unit tests live inline in `src` files under `#[cfg(test)] mod tests`. Larger crates put integration tests in a single `tests/it/` target rooted at `tests/it/main.rs` with one module per area (gateway, gateway-stt); smaller crates use single-file targets such as `tests/paths.rs`. Shared test helpers live in `tests/common/`, fixtures in `tests/fixtures/`. Test names are long descriptive snake_case sentences (e.g. `a_process_lifetime_lease_recovers_after_its_owner_is_terminated`). Cross-crate product tests live in `crates/product-integration-tests`. Nextest is configured in `.config/nextest.toml` with a `heavy` test group (max-threads = 2) for tensor/FFI-heavy crates.
- Directory map: `crates/` holds all workspace member crates plus `shared-ui` (a TypeScript+CSS package excluded from the Cargo glob, consumed by both esbuild-built UIs); `guide/` is the mdbook user guide; `prompts/` holds example prompt pipelines; `tools/` holds Node and PowerShell dev scripts (sidecar staging, MSRV validation); `vibe/` holds dated design records and `archdoc.md`; `local/` holds local run configuration (gateway.toml, profiles, fixtures); `.config/` holds nextest config; `.github/` holds CI workflows and reusable actions; `images/` holds image assets; `target/` and `target-msrv/` are build outputs.
- Component boundaries (per `vibe/archdoc.md`): the executor (promptforge, promptforge-core, promptforge-lua, promptforge-agent, promptforge-parser, promptforge-store, promptforge-tools, promptforge-webfetch, promptforge-web-search, promptforge-model-client, promptforge-tool-picker) parses and runs prompt pipelines and Lua agent programs; the gateway (gateway and gateway-* crates) is an independent server owning model routing, provider access, and local inference; the CLI is a thin shell adapter over the executor; the workshop (workshop, workshop-server) is the Tauri desktop shell and in-process server; the shared substrate (shared-loopback, shared-progress, shared-protocol, shared-sidecar) provides cross-product facilities and depends on nothing. AGENTS.md binds four dependency rules: Gateway crates cannot depend on Workshop or PromptForge product crates; PromptForge crates cannot depend on Gateway or Workshop crates; Workshop crates cannot depend on Gateway crates.
- Conventions summary: Rust edition 2024, MSRV 1.89 pinned in `rust-toolchain.toml` (CI overrides with RUSTUP_TOOLCHAIN). Workspace lints forbid unsafe code outside explicitly owned boundaries, deny clippy `all` plus `unwrap_used`/`expect_used`, and warn on pedantic, missing docs, and missing Debug impls. Behavior changes ship with tests in the same change; structural enforcement (parsers, snapshots, allowlists, count or topology checks) requires explicit user approval per AGENTS.md. Comments explain non-obvious constraints and cite upstream issue URLs for workarounds. Cargo features gate real constraints (toolchain, native builds), never product shape. Runtime and serve paths never compile native dependencies, exit the process, or install process-global state. Long-running work reports through `shared-progress`. Supply chain is gated by `cargo deny check` and `cargo audit` (deny.toml). UI packages (workshop-server/ui, gateway-config-ui/ui) use npm with `npm run typecheck`, `npm run build`, and `npm test`.

</project-survey>

<execution-plan>

## Execution Instructions

<step-1>

### Step 1: Narrow the input tool contract (DEBT-UPM-02) [completed]

- Component: none

In `crates/promptforge-core/src/input.rs`, change `InputTool::description` to "Give the user the opportunity to add a prompt and wait for their typed input.", drop the `question` property from `parameters_schema` so it advertises an empty properties object, and remove the question-channel mention from the `call` doc comment. Do not extend `InputBroker` with a question channel. Update the affected tests in `crates/promptforge-core/src/execute/tests/input.rs` and `crates/workshop-server/tests/it/chat_gate/`, and add a schema test asserting the advertised properties carry no `question` and the description makes no ask-a-question promise. In the same commit, fix `crates/promptforge-agent/AGENTS.md`: the absent-globals rule names `execute`, but the renamed absent global is `call`. Verify with `cargo nextest run -p promptforge-core input` and `cargo test -p workshop-server --test it chat_gate`, then commit as the DEBT-UPM-02 commit.

</step-1>

<step-2>

### Step 2: Flip the version gate to 0-only and migrate prompts (DEBT-UPM-04) [completed]

- Component: none

In `crates/promptforge-core/src/execute.rs`, change the gate arm `Some(SUPPORTED_MAJOR | 0) => {}` to `Some(0) => {}` and delete the dual-read comment. In `crates/promptforge-core/src/execute/support.rs`, remove `SUPPORTED_MAJOR` when no use remains, or repurpose it to name the zero gate. Confirm `crates/promptforge-parser` version handling agrees with the 0-only gate. Migrate every `promptforge: 1` string to `promptforge: 0` across `crates/`, including the inline test strings listed in the implementation contract (the `execute/tests/` files, `execute/context.rs`, `observe.rs`, and `promptforge-parser/src/tests.rs`); exclude `vibe/`. Invert tests that assert a `promptforge: 1` document is accepted into typed `UnsupportedVersion` rejection tests, and add the gate behavior test: a `promptforge: 1` document fails with `UnsupportedVersion` naming the declared version, a `promptforge: 0` document runs, and a versionless document fails with the structure error. The migration and the gate flip land together in this one commit; a flipped gate over un-migrated tests fails the suite. Verify with focused per-crate tests (`cargo nextest run -p promptforge-core`, `cargo nextest run -p promptforge-parser`, `cargo test -p promptforge-lua`, and the workshop-server suite), then the full gate: `cargo build`, `cargo fmt --all --check`, clippy with `-D warnings` (workspace and workshop variants), and the full workspace suite per the project survey's documented commands and host fallback. Commit as the DEBT-UPM-04 commit.

</step-2>

</execution-plan>
